
// Function to find max of two numbers.
function findMax(a, b) {
    return (a > b) ? a : b;
}

// Find max of 10/2 and assign to number.
let number = findMax(10, 2);

// Log the max number.
console.log(number);